from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='attention decoding for subjects with hearing impairment,thus wearing CIs or hearing aids',
    author='semeco',
    license='',
)
